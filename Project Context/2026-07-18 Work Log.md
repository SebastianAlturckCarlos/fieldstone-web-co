# 2026-07-18 — Agentic OS Pickup: Second-Machine Setup & Verification

Picked up the Agentic OS engine (repo `apps/`) from Sebastian. Read `Master_Blueprint.md` + `apps/README.md` first per the handoff instructions, then got the whole thing running and verified on this machine. See [[2026-07-17 Work Log]] for the site-side context.

## Setup gaps found & fixed (committed to the repo)
- **`.env.example` was missing** — `apps/README.md` says `cp .env.example .env`, but the root `.gitignore`'s `.env.*` pattern had swallowed it, so Sebastian's copy never got committed. Recreated it with all-safe defaults (`AGENT_MODE=dry-run`, `SEND_MODE=mock`) covering every env var the code reads, plus a `!.env.example` gitignore exception.
- **`better-sqlite3@^11` won't install on Node 24** — no prebuilt binary for this machine's Node 24.12.0, and the source-build fallback needs the VS C++ toolset (not installed). Bumped to `^12.11.1` (prebuilds for Node 20+, so Sebastian's Node 20.16 is unaffected).

## Run environment on this machine
- Engine runs from **`C:\dev\fieldstone-workspace\apps\`** — a copy of the repo's `apps/`, per the README's cloud-sync warning. Relevant here because the repo sits inside this Obsidian vault and the vault's **Sync core plugin is enabled**: a live SQLite file next to the repo would sit under a sync client.
- Vault digest target `C:\dev\fieldstone-workspace\vault\Financial Growth\` created (matches the hardcoded `VAULT_DIR` in `digest.ts`).
- ⚠️ Two copies of `apps/` now exist: the **repo copy is the source of truth** for code; the workspace copy is the run environment. Port any code change back to the repo.

## Verified end-to-end (dry-run, $0)
- `npm run seed` → 10 KC-metro leads; `npm run tick` → 5 leads `pending → audited → drafted` (QA score 88, ledger at $0.0000 against the $5 cap).
- Second tick processed the rest: 9 drafted total with 2 QA-revision rounds visible ("revised" drafts), 1 failed `no_flaws` — matches Sprint 1's verification exactly.
- `POST /api/approve/1` → `validated`; send job → `sent` (mock-stamped, nothing delivered). Personalized demo links (`?co=&trade=&fix=`) present in every draft.
- Dashboard on `http://localhost:5180` serving, `/api` proxy live, SSE feeding the sphere.
- Gotcha learned: **auto-send runs on the 60s auto-tick interval**, not inside `POST /api/tick` — `POST /api/send` flushes validated emails immediately.

## Project Context moved into the repo
This folder (`Project Context/`) previously lived one level up, in the Obsidian vault but **outside the repo** — so work logs never traveled with a clone or push. Moved into the repo root today (see the folder's new `README.md` for the convention). The repo is inside the vault, so Obsidian still indexes everything and wiki-links keep resolving.

## Sprint 3 completed (same day, second session)
Real send credentials arrived (Resend key, Cal.com link, postal address) → configured in the **workspace `.env` only** (gitignored; the key is send-only restricted — can deliver but not poll status).

- **Deliverability proven:** one test email through `mail.fieldstone-webco.com` to my own inbox — HTTP 200, which also confirms the domain is verified in Resend. Sebastian had already done the DNS work.
- **Reply pipeline built** (`jobs/replies.ts` + endpoints): Reply Desk on the new Pipeline screen (paste a reply → Sales Rep classifies), `POST /api/webhooks/resend` ready for when we tunnel a public URL, unsubscribe → instant suppression, positive → hot-lead alert + follow-up draft (human send click only), >5% bounce day → circuit breaker pauses sends until `POST /api/send/resume`.
- **Pipeline screen built:** funnel with conversion % between stages, Reply Desk, follow-up queue, dense lead table.
- **Safety guards added at dispatch:** test/example addresses never touch the real API, and canned dry-run drafts are refused for real delivery (real sends require `AGENT_MODE=claude-code` so copy is model-written). Verified: approve → send in resend+dry-run returns `0 sent` with the refusal reason.
- **Gotcha:** better-sqlite3 v12 enforces foreign keys by default — broke `npm run reset`; fixed by clearing child tables first and detaching episodic memory instead of deleting it.

## Custom domain cutover (third session — Sebastian's bug reports)
Sebastian reported the new `fieldstone-webco.com` showed a **white page** and that he never got the test email. Both explained and fixed:

- **White page:** DNS was fine (Porkbun → GitHub Pages, custom domain active). The build was the problem — `vite.config.js` hardcoded `base: '/fieldstone-web-co/'` for the github.io subpath, so on the custom domain every asset path 404'd. Fixed to `base: '/'`, added `public/CNAME` (keeps the domain bound across deploys), redeployed. Verified: assets, `/about/`, and the personalized demo route all HTTP 200 on the custom domain; old github.io URLs 301-redirect.
- **Missing email:** test #1 went to *my* address, not Sebastian's — nothing was broken. Test #2 sent to both of us.
- **New finding — no reply path:** `mail.fieldstone-webco.com` has **no MX record**; a prospect hitting "reply" would bounce. Interim fix: `REPLY_TO` env (Resend `reply_to` header) → sebastianrhoton@gmail.com. Proper fix later: Resend inbound or Porkbun email forwarding.
- `SITE_BASE` switched to `https://fieldstone-webco.com` everywhere — outreach demo links are now branded.

## Sprints 5, 6, 7 (fourth session — continuous execution directive)
Operator requested: fix the sphere lag, remove manual ticking, hook up claude-code on this machine, deepen agent collaboration — and run through Sprint 6+ without pausing.

- **Sphere lag diagnosed & fixed:** the real killer was `backdrop-filter: blur(20px)` on every panel over *constantly animating* backgrounds (nebulae/glows/grid/starfield) — every panel re-blurs the whole screen every frame — stacked on a Bloom+Noise+Vignette post chain at DPR 2. Built a 3-tier adaptive quality governor into SynapseCore: real frame-time measurement, auto-step-down below 24fps, software-WebGL detection → static frame, localStorage persistence, hidden-tab pause, `.perf-lean` class that freezes bg animation + drops panel blur. "A few frames a minute" machines now land on a clean static or lean-animated frame automatically.
- **Manual "Run tick" removed** — the engine is fully autonomous (auto-tick, auto-send, scheduled digest + Dev Agent). The human's single surface is the new **CEO Command console** (crown button / CEO click anywhere): approvals, follow-ups, pause/resume, digest/dev-agent triggers.
- **claude-code mode live:** the "refusing real delivery of dry-run drafts" message was the guard doing its job (dry-run copy is canned — it must never really send). The real fix was switching this machine to `AGENT_MODE=claude-code`. Two genuine bugs surfaced: `shell: true` on the CLI shell-out shredded multi-line prompts on Windows (args concatenate unescaped — removed the shell), and no Ollama on this machine (added a probe + Haiku-on-subscription fallback for the researcher). Researcher now audits the real fetched site (`site_fetch.ts`).
- **Sprint 5 (Synapse Immersion) v1:** click the sphere → camera flies inside → spatial org (CEO top, workers below, live pulse traffic on pipeline edges) → click through to agent panels / CEO console; Esc exits.
- **Sprint 6 (JARVIS) v1:** chat + voice panel, whitelisted action protocol (approve/reject/followups/pause/resume/digest/dev-agent), Haiku via claude-code, keyword fallback in dry-run. Verified: real Haiku status summary of live engine state.
- **Collaboration:** new `sales_consult` stage — Sales Rep reviews every CEO-approved draft for click-through (score + notes + subject alternative); weak scores trigger one CMO revision with the notes as feedback. Approval cards show the full agent chain.

## Open items
- [x] Sprint 3 accounts — Resend key + verified domain + booking link + postal address all live (2026-07-18, second session)
- [ ] **First real send day:** import real leads (CSV — open decision), flip `AGENT_MODE=claude-code`, warmup ramp per Phase 1 (10/day)
- [ ] Optional: tunnel `POST /api/webhooks/resend` (e.g. Cloudflare Tunnel) into Resend for automatic open/bounce/reply events — the send-only key can't poll status
- [ ] Open decisions from the blueprint: lead sourcing (manual CSV recommended), auto-approve threshold timing, phone access to the dashboard
- [ ] Commit is local only — push to be done deliberately
- [ ] Task Scheduler entries (07:00 tick / 09:30 send / 18:00 digest / nightly DB backup) not set up yet on this machine
